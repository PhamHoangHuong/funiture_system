import React from 'react';

const ProductList: React.FC = () => {
    return (
        <div>
            <h1>Product List</h1>
            {/* Thêm nội dung danh sách sản phẩm ở đây */}
        </div>
    );
};

export default ProductList;